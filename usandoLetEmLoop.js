for (let i = 0; i <10; i++){ // Com o let ao invés do var essa função só serávisivel dentro desse bloco
    console.log(i)
}

console.log('i =', i) // Não será visível pois o let não deixa visualizar fora do bloco