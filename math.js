const raio = 5.6 // o raio da circunferencia
const area = Math.PI * Math.pow(raio,2) // recebe a área * pow raio elevado ao quadrado

console.log(area.toFixed(3)) // imprime a area, toFixed pra delimitar as casas decimais
console.log(typeof Math) // Para saber o Tipo da constante (objeto)